# 数分精灵 分析逻辑修复总结

## 一、已修复的问题（基于测试结果分析）

### 1. LLM提示词增强 (`llmFullAnalysis`)
**问题**: LLM在多指标、趋势、筛选、HAVING等场景下生成不正确的SQL
**修复**:
- 新增 `allMetrics` 字段：要求LLM列出用户提到的所有指标列名
- 新增 `havingCond` 字段：要求LLM返回HAVING条件表达式
- 增强规则3：趋势问题必须用日期/年份维度，不能用分类列
- 增强规则6：多指标问题必须列出所有指标
- 增强规则7：筛选查询用WHERE不用COUNT聚合
- 新增规则10：率/百分比字段阈值用AVG而非SUM/MAX
- 新增规则11：禁止生成RATIO()函数
- 新增规则12：实体趋势问题需要日期维度+品牌筛选

### 2. 多指标SQL生成 (`generateSQL`)
**问题**: "各品牌的平均价格和总销量"中销量错误使用AVG
**修复**:
- 关键词邻近度检测：检查"平均"/"总"是否紧邻各指标名（±5字范围）
- "平均价格" → AVG(价格)，"总销量" → SUM(销量)
- 同步更新 `parseCompareMetrics` 中的聚合检测逻辑

### 3. 多指标AI模式同步
**问题**: AI增强模式下LLM返回的secondaryMetric未正确构建`_lastMultiMetricFields`
**修复**:
- 处理LLM返回的 `allMetrics` 数组，验证并构建多指标列表
- 兼容旧格式（metric + secondaryMetric）
- 趋势类问题自动切换到日期维度并强制折线图

### 4. RATIO()非法函数修复
**问题**: "各厂商的销量占比是多少"生成 `RATIO(销量(万辆))` 非法SQL
**修复**:
- 新增分组占比模式：`ROUND(SUM(metric) * 100.0 / (SELECT SUM(metric) FROM table), 2) AS 占比百分比`
- 在 `executeQuery` 中正确计算各组占总和的百分比
- 在 `buildChartData` 和 `buildInterpretation` 中使用正确的结果列名

### 5. 图表类型选择 (`getSmartChartType`)
**问题**: 趋势类问题在LLM模式下始终返回'bar'而非'line'
**修复**:
- 同时检查原始用户问题(`window._lastQuery`)中的趋势关键词
- 扩展趋势关键词：增长趋势、变化趋势、从X到Y年等
- 同步检查分布/排名/对比/相关性关键词

### 6. 结果验证增强 (`validateResult`)
**问题**: 未检查多指标完整性和趋势维度正确性
**修复**:
- 多指标完整性：检查结果是否包含所有请求的指标列
- 趋势维度验证：趋势分析的维度必须是日期/时间列
- 自动修复：趋势问题维度错误时自动切换到日期维度

### 7. HAVING条件支持
**问题**: LLM返回的HAVING条件未被使用
**修复**:
- 在AI增强模式中接收并存储 `havingCond`
- 在SQL生成和执行中使用HAVING条件

## 二、修复对照表（修复验证报告中的具体问题）

| 测试问题 | 修复前 | 修复后 |
|---------|--------|--------|
| 各国家的电商渗透率和销售额分别是多少 | 只有渗透率，缺销售额 | allMetrics确保两个字段都返回 |
| 各品牌平均价格和总销量是多少 | 销量用AVG | 邻近度检测：价格→AVG，销量→SUM |
| 各厂商的销量占比是多少 | RATIO()非法函数 | ROUND(SUM*100/(SELECT SUM)) |
| 市场份额超过20%的厂商 | HAVING MAX > 20 | HAVING AVG > 20（LLM规则10） |
| 各国家电商渗透率变化趋势 | 按国家分组，bar图 | 强制按年份分组，line图 |
| Netflix订阅用户数增长趋势 | COUNT(*)无筛选 | 日期维度+平台筛选，line图 |
| 价格在20万以下且续航超500km | COUNT聚合 | WHERE筛选条件，intent=filter |

## 三、未覆盖的数分问题类型

### 已部分覆盖但需加强的类型
| 类型 | 现状 | 改进方向 |
|------|------|---------|
| 同比/环比 | 有window_function意图 | 需验证LLM是否正确生成LAG窗口SQL |
| 累计/移动平均 | 有window_function意图 | 移动平均未明确支持 |
| 多维交叉分析 | 有multiDimensions支持 | 需测试"各渠道各产品类别"场景 |
| 区间筛选 | 有rangeFilter | 需验证"X在A到B之间"的识别率 |
| 条件计数 | 有count聚合 | 需验证"超过X的有几个"场景 |
| 中位数/分位数 | parseAggregation支持 | 需验证LLM是否返回正确aggregation |

### 完全未覆盖的类型
| 类型 | 示例问题 | 建议实现方案 |
|------|---------|------------|
| 百分比变化 | "2024年比2023年增长了多少%" | 新增intent=percentageChange，用窗口函数计算 |
| 数据概览查询 | "给我一个数据概览" | 新增intent=overview，自动选择关键字段统计 |
| 缺失值检查 | "数据中有多少缺失值" | 新增intent=dataQuality，扫描null/空值 |
| 数据描述性统计 | "数据的基本统计信息" | 新增intent=describe，输出count/mean/std/min/max |

## 四、测试建议

建议使用以下新增测试用例验证修复效果：
1. "各品牌的平均价格和总销量是多少" → 验证AVG/SUM正确
2. "各厂商的销量占比是多少" → 验证百分比SQL合法
3. "各国家电商渗透率从2020到2024的变化趋势" → 验证line图+年份维度
4. "Netflix订阅用户数的增长趋势" → 验证筛选+趋势
5. "市场份额超过20%的厂商有哪些" → 验证HAVING AVG
6. "价格在20万以下且续航超过500km的BEV车型有哪些" → 验证WHERE筛选
